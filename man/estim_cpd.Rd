% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estim.R
\name{estim_cpd}
\alias{estim_cpd}
\title{Country Product Dummy (CPD) regression model}
\usage{
estim_cpd(
  data,
  region = "region",
  product = "product",
  price = "price",
  base.region = NULL,
  output = "SPPP"
)
}
\arguments{
\item{data}{Data frame, data table or tibble containing at least three
columns identifying region, product and individual item-level price quotes}

\item{region}{Identifier for regions (within or across countries)}

\item{product}{Product identifier}

\item{price}{Individual item-level price quotes; duplicated region-product
pairs are aggregated by way of averaging across region-product pairs.}

\item{base.region}{A character specifying the base to which the estimated
logarithmic regional price levels are expressed.
When NULL, they refer to the (unweighted) regional average,
similar to \code{contr.sum()}.}

\item{output}{Either "SPPP", which returns the estimated subnational
purchasing purchasing power parities, that is,
\mjseqn{\hat{SPPP}_r = exp(\hat{\alpha}_r)} or
"Std. Error", which returns the standard errors of the estimation;
default is "SPPP"}
}
\value{
A tibble (scalar)  with estimated subnational
purchasing purchasing power parities, that is,
\mjseqn{\hat{SPPP}_r = exp(\hat{\alpha}_r)}  if \code{output = "SPPS"} (default),
or vector containing the standard errors of the estimation
if \code{output = "Std. Error"}
}
\description{
\loadmathjax
\code{estim_cpd} in  \pkg{OECDsppps} creates ...;
see \emph{Details} and
\insertCite{worldbankMeasuringRealSize2013;textual}{OECDsppps},
for more information.
}
\details{
\strong{detailed} description goes
\mjseqn{i}, \mjseqn{P_{i}},
here
\mjdeqn{ratio-to-average = p_{ij}/\mu_j}{ratio-to-average = p_{ij}/\mu_j}
}
\examples{
suppressPackageStartupMessages(library(dplyr))
df <- tibble(
  region = as.factor(c(1, 2, 1, 2)),
  product = as.factor(c(1, 1, 2, 2)),
  price = c(25, 28, 23, 26)
)

estim_cpd(df)
estim_cpd(df, output = "Std. Error")

}
\references{
\insertAllCited{}
}
