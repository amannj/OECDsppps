% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valid.R
\name{valid_outlier_plot}
\alias{valid_outlier_plot}
\title{sPPPs outlier plot}
\usage{
valid_outlier_plot(
  data,
  sPPPs = "sPPP",
  title = NULL,
  facet_var = NULL,
  facet_ncol = NULL,
  facet_scale = "fixed",
  bins = 70,
  outlier_cutoffs = c(1.5, 0.5),
  xlim_range = NULL
)
}
\arguments{
\item{data}{A data frame or tibble containing at least one column with
the subnational Purchasing Power Parity indices}

\item{sPPPs}{Vector with subnational Purchasing Power Parities}

\item{title}{Option to add a plot title; default is NULL}

\item{facet_var}{Option to wraps a 1d sequence of panels into 2d based on
the provided variable following ggplot2's \code{facet_wrap()}; default is NULL}

\item{facet_ncol}{Option to change the number of column of the created
facets following ggplot2's \code{facet_wrap()} argument \code{ncol};
default is 2 if \code{facet_var} is used}

\item{facet_scale}{Option to change wheterhe the facet scales should be fixed
(\code{"fixed"}, the default), free (\code{"free"}), or free in one
dimension (\code{"free_x"}, \code{"free_y"}); default is \code{"fixed"}}

\item{bins}{Number of bins following ggplots' \code{geom_histogram()} argument \code{bins};  Default is \code{70}}

\item{outlier_cutoffs}{Cutoffs to highlight potential outliers in
the plot and need to be provided as a vector
as \code{outlier_cutoffs = c(upper_limit, lower_limit)};
default is \code{1.5} and \code{0.5}, i.e. \code{outlier_cutoffs = c(1.5, 0.5)}}

\item{xlim_range}{Limits for the x and y axes, following
ggplot2's \code{coord_cartesian()} argument and need to be provided as a vector
as \code{outlier_cutoffs = c(upper_limit, lower_limit)}; default is NULL}
}
\description{
\code{valid_outlier_plot()} produces some simple validation plots to check
subnational PPP estimates for potential outliers
}
\examples{
\dontrun{
uk_cpi |>
  select(Year,
       region = "Region",
       product = "Product code",
       price = "Reference quantity price"
  ) |>
  mutate(
    region = as.factor(region),
    product = as.factor(product)
  ) |>
  estim_cpd() |>
  valid_outlier_plot(title = "sPPPs outlier with adjusted outlier cutoffs",
                     # Adjust outlier cutoffs (default is 1.5 and 0.5)
                     outlier_cutoffs = c(1.1, 0.9))
}

}
